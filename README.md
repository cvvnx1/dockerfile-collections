# How to login to docker hub on ubuntu

```shell
sudo apt install -y gnupg2 pass
gpg2 -k
pass init "whatever key id you have, Ex. docker"

docker login -u cvvnx1
```

# Dockerfile-collections

Dockerfile collections
